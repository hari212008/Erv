apiVersion: v1
kind: PersistentVolume
metadata:
  name: nfs-pv
spec:
  capacity:
    storage: 1Gi
  accessModes:
  - ReadWriteMany
  nfs:
    path: /efs
    server: 10.160.71.107
  persistentVolumeReclaimPolicy: Retain
  storageClassName: nfs
